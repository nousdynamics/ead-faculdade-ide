# Screenshots de referência/verificação (pesados, não versionar)
_capture/ref/
_capture/shot-*.png
_capture/*.png

# Vercel (link local do projeto)
.vercel

# Conta CMS (hash de senha — gerado em runtime)
data/cms/account.json

.vercel
.env*
